#!/usr/bin/env ruby
require 'colsole'
require 'pretty_trace/enable-trim'
include Colsole

require 'jobly'
require 'jobly/boot'

PrettyTrace.filter [
  %r[mister_bin],
  %r[bin/jobly],
  %r[bin/ruby],
  %r[lib/ruby],
  %r[lib/jobly],
]

router = Jobly::CLI.router
exit router.run ARGV
